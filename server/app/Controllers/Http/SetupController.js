'use strict'

class SetupController {
    async setup({request}) {
        return 'Some test';
    }

    async addInstructor({request}) 
    {
        
    }

    async addSection({}) {
        
    }
}

module.exports = SetupController
